{{ include('global/global_header.twig', {title: 'Page02'}) }}

<section class="page_wrapper">
    <!-- HEADER -->
    {{ include('modules/header/main_header.twig') }}

    <!-- MAIN CONTENT -->
    <main>
        <div class="wrapper">
            <p class="x20">FORM ITEMS</p>
            {{ include('modules/partials/forms/materialize/demo.twig') }}
        </div>
    </main>

    <!-- FOOTER -->
    {{ include('modules/footer/main_footer.twig') }}
</section>

{{ include('global/global_footer.twig') }}